# Extension builtin symbols intentionally present only in NeoVM.
# One symbol per line.
#
# Policy note:
# - these names must be absent (`fboundp => nil`) in GNU Emacs oracle runs
# - these names must be present (`fboundp => t`) in NeoVM runs
# - each symbol should also have an explicit vm-compat behavioral corpus entry
#   in `cases/neovm-only.list`
string-chop-newline
string-fill
string-limit
string-pad
